<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}}</title>
    <link rel="stylesheet" href="/public/styles.css" />
    {{{head}}}
  </head>
  <body>
    <div class="container">
      <header>
        <h1>{{title}}</h1>
      </header>
      <nav>
        <a href="/" class="{{#if isHome}}active{{/if}}">Home</a>
        <a href="/realtimeproducts" class="{{#if isRealtime}}active{{/if}}">Tiempo real</a>
      </nav>
      {{{body}}}
      <footer>
        <div class="muted">Express + Handlebars + Socket.IO</div>
      </footer>
    </div>
    {{{scripts}}}
  </body>
</html>